{
  "name": "dsh-openbao-shell-env",
  "version": "0.1.0",
  "description": "DSH plugin: inject the OpenBao LDAP agent password into every model shell call as DSH_OPENBAO_LDAP_AGENT_PASSWORD via the dsh-shell-env trusted registry (host process.env is scrubbed of KEY|PASSWORD|SECRET|TOKEN names, so the password cannot reach the agent any other way).",
  "type": "module",
  "main": "index.js",
  "license": "MIT"
}
